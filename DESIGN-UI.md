# WallFlow 小程序 UI 设计文档

> 范围: 首页信息流 / 分类页 / 详情页(信息架构 + 交互状态)
> 版本: v1.0(2026-08-10)| 对应实现规格: `specs/09-miniprogram-frontend.md`
> 前置文档: 设计文档(已批准)| ENG-PLAN.md | SPEC-MVP.md

---

## 1. 设计原则

| # | 原则 | 含义 |
|---|------|------|
| 1 | **壁纸即产品,UI 让路** | 图片占最大视觉面积,界面 chrome 退到背景。沉浸感是壁纸类产品的第一体验。 |
| 2 | **3 步最短路径** | 从打开到保存不超过 2 次点击: 信息流 → 详情 → 保存。任何页面都不为"找到喜欢的壁纸"设置多余门槛。 |
| 3 | **信任即品牌** | 免费 + 开源 + 来源透明是产品定位,许可徽章(CC0/CC BY/PD)时刻可见,不藏在二级菜单 — 这是与付费墙竞品的差异化资产。 |
| 4 | **拇指优先** | 主操作(保存)固定在底部拇指热区,单手可完成全部核心动作。 |
| 5 | **状态可见** | 每个异步动作有明确状态(骨架/成功/失败/空/到底),永不静默失败,失败必有重试路径。 |

---

## 2. 视觉基调

### 2.1 色彩(明亮为默认,深色可切换)

| Token | 明亮模式(默认) | 深色模式 | 用途 |
|-------|---------|---------|------|
| `--bg` | `#F5F5F3` | `#0B0C0F` | 页面背景(默认明亮清新) |
| `--card` | `#FFFFFF` | `#16171C` | 卡片底 |
| `--text-1` | `#1A1A1A` | `#FFFFFF` | 主文字 |
| `--text-2` | `#1A1A1AA6` | `#FFFFFFB3` | 次级文字 |
| `--cta-grad` | `#4A6CFF → #7A5CFF` | `#6E8CFF → #8A6CFF` | 主 CTA 渐变(紫蓝,明亮下略深,克制不喧宾) |
| `--cc0` | `#0FA47C` | `#2ECC9F` | CC0 许可徽章(青绿=自由) |
| `--ccby` | `#2B6FDD` | `#4A90FF` | CC BY 徽章(蓝=署名) |
| `--pd` | `#6B7280` | `#9AA0A6` | Public domain 徽章(灰) |
| `--danger` | `#E5484D` | `#FF5C5C` | 错误/举报 |

### 2.2 形状与动效

- **圆角**: 卡片 16rpx;按钮/标签 999rpx(胶囊)
- **按压反馈**: 卡片 `transform: scale(0.97)` 120ms
- **页面过渡**: 详情页淡入 + 轻微上移 300ms(微信 `navigateTo` 默认动画之上叠加)
- **收藏动效**: 心形 空心→实心 弹簧放大回缩
- **保存成功**: 按钮短暂变"✓ 已保存"再恢复,toast「已保存到相册」

### 2.3 字体

系统字体栈;标题 700 字重;图片信息用 13-14px 次级字。不引入第三方字体(小程序包体积 + 加载成本)。

---

## 3. 信息架构

### 3.1 全局导航(tabBar,3 tab)

```
┌─ tabBar ───────────────────────────────┐
│  [发现]      [分类]      [我的]          │
│  首页信息流   分类浏览    收藏/关于/设置   │
└─────────────────────────────────────────┘
```

### 3.2 页面层级

```
tabBar 首页(发现)
  ├─ 顶部搜索条(点击 push → 搜索页)
  ├─ 每日精选 banner(内容位)
  └─ 双列瀑布流 ──▶ 详情页(push)
                        ├─ 底部操作栏: 收藏/保存/分享
                        ├─ 相似推荐卡条 ──▶ 新详情页(push)
                        └─ 更多: 设为壁纸引导 / 举报 / 原图链接
tabBar 分类
  ├─ 顶部横向分类 chips(含「全部」)
  └─ 双列瀑布流 ──▶ 详情页
tabBar 我的
  ├─ 收藏夹(网格,可取消)──▶ 详情页
  └─ 设置/关于(开源说明/许可解释/反馈/举报)
push 搜索页
  ├─ 搜索历史 + 热门词
  └─ 结果双列流 ──▶ 详情页
```

### 3.3 数据流

```
首页/分类 ── GET /wallpapers(category?, cursor) ──▶ 双列流
详情页   ── GET /wallpapers/:id + /wallpapers/:id/similar
图片     ── COS 签名直链(短时效,下载域名白名单,无上游依赖)
收藏     ── POST/GET/DELETE /favorites(已登录)
详情收藏态 ── 已登录时 `GET /wallpapers/:id` 返回 `is_favorited`(初始态,未登录默认 false)
埋点     ── POST /events(匿名可上报)
```

---

## 4. 页面设计

## 4.1 首页信息流

### 布局线框

```
┌─────────────────────────────┐
│ 🔍 搜索壁纸、风格、关键词…  │ ← 顶部固定搜索条(点击跳搜索页)
├─────────────────────────────┤
│  [最新]  [热门]             │ ← 排序 Tab(2026-08-15): latest=新到旧(默认)
│                             │    hot=7 天行为热度(sort=hot,加权: 下载成功5/收藏3/点击2)
│ ┌─────────────────────────┐ │
│ │   今日精选  (大图 banner) │ │ ← 静态运营位(人工配置单张精选图)
│ └─────────────────────────┘ │
├─────────────┬───────────────┤
│ ┌─────────┐ │ ┌─────────┐   │
│ │         │ │ │         │   │
│ │  壁纸图  │ │ │  壁纸图  │   │ ← 双列瀑布流
│ │         │ │ │         │   │   图片按原始比例(用 width/height)
│ ├─────────┤ │ ├─────────┤   │   卡片 = 图 + 1 行信息
│ │ 标题 [CC0]│ │ │ 标题 [CC BY]│   │   右下角 = 许可文字徽章(CC0/CC BY/PD)
│ └─────────┘ │ └─────────┘   │
│ ┌─────────┐ │ ┌─────────┐   │
│ │  …更多  │ │ │          │   │
│ └─────────┘ │ └─────────┘   │
├─────────────┴───────────────┤
│       加载中… / 已经到底啦    │
└─────────────────────────────┘
```

### 交互状态矩阵

| 状态 | 触发 | 表现 | 重试/出口 |
|------|------|------|-----------|
| **首载** | 进入页面 | 双列 6 块骨架屏(shimmer 灰块,同卡片比例) | — |
| **分页加载** | 上拉触底 | 底部占位「加载中…」(胶囊 spinner) | — |
| **到底** | 无更多数据 | 「— 已经到底啦 —」+ 该分类数量 | — |
| **下拉刷新** | 下拉 | 微信原生下拉;成功后新数据顶入 | 失败 toast,数据保留 |
| **空** | 后端返回空 | 空态插画 + 「这里还没有壁纸」 | 「刷新」按钮 |
| **网络错误** | 请求失败 | 全屏错误态 + 「网络开小差了」 | 「点击重试」按钮 |
| **图片加载失败** | 单图 404 | 卡片内灰块 + 重试角标(单图级,不整页崩溃) | 点击角标重载 |

### 关键交互

- **卡片点击** → push 详情页;进入前上报 `preview_click`
- **搜索条**: 仅入口,不做就地输入(避免与搜索页双实现)
- **懒加载**: `image` 组件 `lazy-load`,占位色 `--card`
- **keyset 分页**: 上拉加载下一页;`cursor` 由后端返回

---

## 4.2 分类页

### 布局线框

```
┌─────────────────────────────┐
│ [全部] [风景] [极简] [萌宠]   │ ← 顶部横向滚动 chips
│ [动漫] [城市] [星空] [自然]…  │   8-12 个,可横向滑动
├─────────────┬───────────────┤
│ ┌─────────┐ │ ┌─────────┐   │
│ │         │ │ │         │   │
│ │  …同上   │ │ │          │   │
│ └─────────┘ │ └─────────┘   │
│             │               │ ← 双列流(同首页组件)
├─────────────┴───────────────┤
│       加载中… / 已经到底啦    │
└─────────────────────────────┘
```

### 交互状态

| 状态 | 表现 |
|------|------|
| chip 未选中 | 次级底色 + 主文字 |
| chip 选中 | 品牌渐变填充 + 白字(非纯下划线,便于拇指点按) |
| 切换分类 | 内容区先清空 → 骨架 → 新数据;上报 `search_click`(分类即搜索的一种) |
| 空分类 | 「该分类暂无壁纸」+ 「看看其他分类」按钮(回「全部」) |
| 错误/到底 | 同首页 |

### 设计决策

- **顶部 chips 而非左侧边栏**: 分类 8-12 个时侧边栏会压缩双列图片宽度;chips 横向滚动保留全宽图片,且与首页共用瀑布流组件。
- **分类数据**: `GET /categories`(回填计数),MVP 前端可先用固定词表 + 计数兜底。

---

## 4.3 详情页(核心页)

### 布局线框

```
┌─────────────────────────────┐
│  ──沉浸大图区(约 65vh)────  │ ← 状态栏透明(navigationStyle: "custom"),图 contain
│                             │   双击缩放: 小程序 = previewImage / Web = 页内手势
│                             │
│                             │
├─────────────────────────────┤
│ 标题 Mountain Lake Sunrise  │
│ [风景] [山] [湖泊]           │ ← 标签 chips
│ 由 @JaneDoe 创作   [CC0]    │ ← 摄影师 + 许可徽章
│          查看许可协议 →      │ ← 弹窗:许可说明/署名要求/原图链接
├─────────────────────────────┤
│ 更多同风格                   │
│ [图][图][图][图] →           │ ← 相似推荐横向滚动(#6)
├─────────────────────────────┤
│  ♡        [ 保存壁纸 ]   ↗  │ ← 底部固定操作栏(毛玻璃)
│  收藏       主CTA      分享  │
└─────────────────────────────┘
```

### 保存流程状态机(关键)

> 解锁门槛(2026-08-15 平衡策略,见 TODO): 已开通流量主时,点「保存壁纸」先看激励视频;
> 广告失败(fill rate 低)按每日限次免费降级(`FREE_FALLBACK_ON_AD_ERROR` + `FREE_FALLBACK_DAILY_LIMIT`,
> 默认 3 次/日,本地按自然日计数)→ toast「广告未加载,已免费解锁(今日剩 N 次)」后照常保存;
> 当日次数用尽或关闭开关 = 严格付费墙(提示稍后重试)。未开通流量主 = 全免费直接保存。

```
         ┌──────────┐  点「保存壁纸」
         │   idle   │──────────────────┐
         └──────────┘                  ▼
   ┌─────────────── wx.getSetting 检查相册授权 ───────────────┐
   │                                                          │
已授权                                                     未授权
   ▼                                                          ▼
┌────────┐  下载+保存(wx.downloadFile→   ┌─────────┐  wx.authorize
│ saving │  wx.saveImageToPhotosAlbum)   │request  │
└────────┘                                └─────────┘
   │ 成功                                    │同意      │拒绝
   ▼                                        ▼          ▼
success「已保存到相册」                   ┌─────────┐  ┌─────────────┐
(按钮短暂 ✓,toast)                      │ saving  │  │ guide dialog │
   │                                      └─────────┘  │「开启相册权限 │
   │ 失败(下载/保存异常)                     │成功       │  才能保存壁纸」│
   ▼                                        ▼          │ [去设置]     │
error toast「保存失败」+ 按钮恢复可点         success    └─────┬───────┘
(可再次点击重试)                                          设置页返回 → 重新检查授权
```

**状态细节**:

| 状态 | 按钮表现 |
|------|---------|
| idle | 「保存壁纸」渐变 CTA,常亮 |
| saving | 按钮内 spinner + 「保存中…」,禁用防连点 |
| success | 短暂「✓ 已保存」+ toast;埋点 `download_success` |
| 授权被拒 | 弹窗引导 →「去设置」(打开设置页)→ 返回后自动重试;不强迫,可关闭 |

**防误触**: 保存中禁连点;下载前校验 `wallpaper_id` 合法(404 时 toast 并返回列表)。

### 收藏 / 分享 / 更多

- **收藏**: 心形 toggle(空心→实心 + 弹动);初始态取详情接口 `is_favorited`(未登录 false);未登录点按 → 静默登录后重试,失败 toast;上报 `favorite_add`
- **登录时机**: 静默登录(`wx.login`→`POST /auth/login`)在 app 启动时执行,收藏/保存前已是登录态,不阻塞交互
- **分享**: `wx.shareAppMessage` 自定义封面 = 当前壁纸图 + 标题;`imageUrl` 用**完整绝对 URL**(COS 签名直链),COS 域名在 `downloadFile`/`request` 白名单内;入口在底部操作栏右侧胶囊
- **更多(右上 ⋯)**: 设为壁纸引导(弹窗: 分步说明「保存后到 系统相册 → 设为壁纸」,因小程序无直接设壁纸 API)、举报(弹窗选原因 → 提交中 loading → 成功 toast / 失败可重试;限流由后端管)、复制原图链接(剪贴板)
- **来源/作者链接可达性(2026-08-15 已定论,见 TODO)**: 详情页不嵌 web-view — 来源/作者 URL 通过「署名与许可(复制)」`wx.setClipboardData` 以纯文本给出(CC BY 署名含许可 URI,可粘贴浏览器打开;剪贴板不涉及合法域名白名单)。若未来要做页内跳转: 微信官方 `web-view` 业务域名**不开放给个人主体**且域名需 ICP 备案 + 校验文件验证所有权(commons.wikimedia.org 为第三方域名,无法绑定业务域名),可选的替代方案是自建中转页(需备案域名)或 `wx.openEmbeddedMiniProgram`(仅跳小程序,不适用)

### 相似推荐

- 「更多同风格」横向卡条,点击 push 新详情页;**页面栈管理**: 微信 `navigateTo` 栈上限 10 层,进入相似推荐详情时若栈深 ≥ 8 改用 `redirectTo` 替换当前页
- 数据: `GET /wallpapers/:id/similar`(#6,标签重叠排序)

### 加载/错误态

| 状态 | 表现 |
|------|------|
| 详情加载 | 大图骨架 + 信息区骨架行 |
| 图片加载失败 | 大图区灰块 + 「图片加载失败 重试」 |
| 接口失败 | 整页错误态 + 重试 |

---

## 4.4 搜索页(简述,支撑信息架构)

- 进入自动聚焦输入;空输入时显示: 搜索历史(本地缓存,可清空)+ 热门词 chips
- 输入防抖 300ms;结果双列流(同首页组件)
- 空结果:「没有找到相关壁纸」+ 换词建议(同义词表)
- 埋点: `search_exposed`(进入)/ `search_click`(提交词)
- 搜索实现见 `specs/05-search.md`(jieba FTS)

## 4.5 我的页(简述)

- 头部: 匿名标识(openid 哈希,不展示真实身份)+ 保存总数(来自埋点,激励感)
- 收藏夹: 网格视图,长按/编辑模式可批量取消
- 关于: 开源理念说明、许可协议解释(CC0/CC BY 是什么,用户信任教育)、反馈、举报
- 设置: 主题模式切换(浅色/深色/跟随系统)

## 4.6 Web 客户端(React + Vite + Tailwind + Service Worker)

> 用户确认(2026-08-10): 产品同时支持 Web。与小程序共享同一后端 API 与设计体系(同一 token 机制、同一批接口、同一套 CSS 变量;身份来源不同: 小程序 openid / Web 匿名设备)。

**技术栈**: React 18 + Vite + TypeScript(strict)+ Tailwind CSS + React Router;PWA: manifest + Service Worker。

**布局(响应式)**:
- 桌面: 3-4 列瀑布流栅格 + 顶部/侧边导航
- 平板: 3 列;手机: 2 列(与小程序一致的双列流)
- 键盘: ←/→ 切换壁纸、Enter 保存、Esc 关闭 lightbox

**页面**: `/` 首页 · `/category` 分类 · `/search` 搜索 · `/detail/:id` 详情 · `/favorites` 收藏

**关键交互**:
- **详情大图 = 页内手势**(用户已拍板): 触摸滑动切换、双指/滚轮缩放、双击复位;移动端也可唤起系统全屏
- **主题**: 明亮默认 + 顶部切换按钮(`localStorage` 持久化 + `prefers-color-scheme` 初始检测)
- **保存**: `fetch → blob → a[download]`(桌面);移动端系统下载;离线时由 SW 提供已缓存图
- **分享**: Web Share API / 复制链接
- **鉴权(已拍板)**: 匿名设备 token(`POST /auth/anon`,localStorage 持久化,收藏按设备)

**Service Worker(离线策略)**:
- 预缓存应用壳(HTML/JS/CSS/图标)→ 二次打开秒开
- 图片运行时缓存(Cache API,LRU 限额 ~50MB)→ 离线可看已浏览壁纸
- 网络优先 + 缓存回退策略(壁纸接口);stale-while-revalidate(静态资源)

**与小程序差异总结**: 仅交互载体不同(页内手势 vs previewImage、键盘 vs 触摸、浏览器下载 vs 相册保存);信息架构、数据流、埋点事件完全一致。

---

## 5. 埋点映射

| 事件 | 触发点 | 设计对应 |
|------|--------|---------|
| `preview_click` | 卡片点击进详情 | 首页/分类/搜索/收藏 共用 |
| `search_exposed` | 进入搜索页 | 漏斗起点之一 |
| `search_click` | 提交搜索词 / 切换分类 | 漏斗第二环 |
| `download_click` | 点「保存壁纸」 | 漏斗第三环 |
| `download_success` | 保存成功 | 漏斗终环 + 转化率 |
| `favorite_add` | 收藏成功 | 留存信号 |

漏斗: `search_click → preview_click → download_click → download_success`(对应 `specs/10-analytics.md`)

---

## 6. 主题模式策略(默认明亮,可切深色)

- **默认明亮清新**;提供深色沉浸切换(用户可调整)
- 小程序: `darkmode: true` + `themeLocation`;默认跟随系统,设置页提供手动切换(浅色/深色/跟随系统)
- Web: 切换按钮 + `localStorage` 持久化 + `prefers-color-scheme` 初始检测
- 所有颜色走 CSS 变量(2.1 表),不写死;图片不做滤镜处理(壁纸即内容)

---

## 7. 微信平台规范

- 点击热区 ≥ 88rpx(44px 标准)
- 主文字与背景对比度 ≥ 4.5:1(`--text-2` 仅用于非关键信息)
- 保存授权使用标准 `wx.authorize` + `scope.writePhotosAlbum`;被拒走设置页引导(非诱导)
- `downloadFile`/`request` 合法域名 = **COS 域名**(或备案 CDN 域名,见 #11)
- 图片 URL 全部为 COS 签名直链,**禁止任何海外上游直链**

---

## 8. 决策记录与开放问题

### 已拍板(2026-08-10)

| # | 决策 | 结论 |
|---|------|------|
| 1 | 视觉方向 | **默认明亮清新,用户可切换深色沉浸** |
| 2 | 首页精选 banner | **静态运营位**(人工配置单张精选图) |
| 3 | 卡片许可徽章 | **文字徽章**(CC0/CC BY/PD) |
| 4 | 详情大图交互 | **小程序 = `wx.previewImage` 全屏预览;Web = 页内手势** |
| 5 | 内容图源 | **精选 CC0/CC BY 语料转存国内 COS**(Wikimedia 大陆不可达,jsDelivr/Gitee 实测不可靠) |
| 6 | Web 端登录 | **匿名设备 token**(`POST /auth/anon`,收藏按设备) |

### 开放问题(新增)

1. **Web 端是否首发**: 与小程序同步上线,还是小程序先行?(影响 #12 排期)

---

## 9. 与规格 #9 的衔接

本设计文档是 `specs/09-miniprogram-frontend.md` 的实现参考。建议在开工 #9 前:

1. 按第 8 节拍板 4 个开放问题
2. 用本设计的 5 态矩阵核对 #9 验收标准(当前标准已覆盖首载/保存/错误,补「到底」「空态」验收)
3. 埋点事件名以本设计第 5 节为准(#10 已引用相同事件名)
